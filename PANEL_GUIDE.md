# راهنمای پنل ربات پالادیوم

## ورود
- کاربر: `/start`
- مالک/مدیر: `/admin`
- افزودن ادمین‌های گروه: مالک ربات را به گروه اضافه کند و بنویسد: `پیکربندی`

## تنظیمات سریع داخل پنل
از بخش ⚙️ تنظیمات، هر مقدار را به شکل `key=value` بفرستید:

- `owner_id=123456` انتقال مالک
- `challenge_channel=@challenge_channel` کانال چالش
- `match_channel=@match_channel` کانال رل‌یابی
- `ai_free_enabled=1` فعال‌سازی AI رایگان
- `ai_free_endpoint=https://example.com/free-ai` آدرس سرویس AI رایگان

## دسته‌بندی‌ها
از بخش 🧩 دسته‌بندی‌ها:

- `order=ticket,challenge,match` تغییر ترتیب
- `challenge=متن جدید چالش` تغییر متن ورودی چالش
- `match=متن جدید فرم رل‌یابی` تغییر متن ورودی رل‌یابی

## عضویت اجباری
از بخش 🔐 عضویت اجباری:

`@channel|نام کانال|ticket,challenge,match`

## اخطار مدیران
از بخش 👮 مدیران:

`ADMIN_NUMERIC_ID|علت اخطار`

وقتی تعداد اخطار به سقف برسد، ربات پیشنهاد محدودسازی یا عزل مدیر را نمایش می‌دهد.

## بکاپ
از بخش 💾 بکاپ/آپدیت، کلمه `BACKUP` را بفرستید.
